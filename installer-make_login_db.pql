PROCEDURE      INSTALLER.MAKE_LOGIN_DB:T  (157549,48274)  |Last amended Feb 20, 2014,13:24:34
program
ifTHEN (fileis( DSN( 'LOGIN_FOLDER') + 'login.SR1') EQ 1)
. DISPLAY INFOBOX 'LOGIN DATABASE ALREADY EXISTS.  WILL NOT CREATE A NEW ONE.'
. WRITE // 'LOGIN DATABASE ALREADY EXISTS' //
. | DO NOTHING AS LOGIN DB EXISTS
ELSE
. EXECUTE DBMS "CALL SCHEMAS.LOGIN"
ENDIF
END PROGRAM
END PROCEDURE
